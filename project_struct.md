# 基于结构化常识注入的端到端安全驾驶模型

## 一、核心思想

将端到端自动驾驶在长尾场景下的安全泛化问题，解耦为两个独立的子问题：

- **物理安全边界**：确定性、可数学表达，需要零误差的规则硬约束，不可用网络近似。
- **驾驶常识认知**：模糊性、依赖经验，需要借助大语言模型（LLM）的世界知识，但必须建立在无噪声的结构化感知之上。

我们主张“**感知完全结构化 → 物理约束引擎 + LLM 常识蒸馏 → 注入端到端规划器**”的安全常识注入范式。

## 二、方案架构（三层协同）

| 层级 | 名称 | 解决的问题 | 技术手段 | 数据来源 |
|:---|:---|:---|:---|:---|
| 第一层 | **硬约束层** | 物理上绝对不能做什么，主动探测碰撞/失稳风险 | 运动学公式 + 碰撞检测规则引擎 | nuScenes GT 标注 + 物理计算 |
| 第二层 | **常识层** | 这个场景有什么潜在危险（尤其长尾语义危险） | LLM 常识推理与离线标注 | GT 结构化信息 → 案情简报 → LLM |
| 第三层 | **驾驶脑** | 在安全边界内做出最优轨迹规划 | VAD（向量化端到端规划模型）+ 物理硬约束头 + 常识蒸馏头 | 真实图片 + 前两层标签联合监督 |

三层并非串行流水线，而是并行协作：硬约束层划定绝对不能逾越的动作空间并探测碰撞风险，常识层赋予模型对长尾危险的预判本能，驾驶脑在双重保护下做出安全且高效的轨迹规划。

## 三、基础模型：VAD 系列

- **模型**：VAD 系列（VAD/VADv2），将地图和障碍物抽象为向量化表征（agent query、map query），规划头输出未来轨迹（waypoints）。
- **当前基底**：VAD（ICCV 2023），确定性轨迹输出，`vad2/` 目录中的默认训练路径。
- **备选基底**：VADv2（锚点词汇表式概率规划），代码完整但需另行配置（CARLA 数据、锚点词表）。
- **方案定位**：本文案是模型无关的增强框架——物理硬约束头与常识蒸馏头并联于规划 decoder 之上，可适配 VAD 系列任一基底。
- **选择原因**：
  1. 开源成熟，社区活跃，有官方预训练权重。
  2. 向量化表征天然契合我们的结构化信息提取思路。
  3. 作为被广泛引用的基线，清晰锚定我们的贡献增量。

## 四、系统化数据挖掘管线（三阶段）

### 第一遍：全量预计算场景标签索引

- **目的**：为后续快速检索和筛选打基础。
- **做法**：对 nuScenes 训练集每一帧，离线提取结构化摘要并存储为轻量级元数据库。
- **提取内容**：
  - 自车状态：速度、加速度、方向盘转角变化率。
  - 周围物体统计：各方位物体数量、最近物体距离、相对速度。
  - 道路结构：当前车道类型、前方路口距离、信号灯状态。
  - 可见性：各象限是否存在视线遮挡。
- **同步生成物理约束头标签**：
  - 每个周围物体的 TTC（相对距离 / 相对速度 + 安全缓冲）。
  - 最大安全曲率 κ_max = μ·g / v²。
  - 最大安全减速度 a_brake_max = -5.0 m/s²（常规路面，雨天可下调）。
  - 紧急制动概率 P_AEB：若存在物体 TTC < 0.5s 且无横向避让空间 → 1，否则 → 0。

### 第二遍：危险基元触发 + 初步筛选

- **目的**：从全量数据中捞出所有候选危险帧。
- **危险基元库**（示例）：
  - `occlusion`：被遮挡的弱势道路使用者正横穿自车路径。
  - `ttc_violation`：与任何物体的 TTC < 2s。
  - `blind_spot_invasion`：盲区内有物体快速接近。
  - `intent_conflict`：前车突然急刹，或旁车无灯变道。
  - `cut_in_risk`：旁车打灯并横向靠近自车道。
  - `red_light_violation`：接近红灯路口且未减速。
- **输出**：触发任一基元的帧构成“候选危险样本池”。
- **时间回溯**：每个触发帧向前回溯 2~3 秒（40~60 帧），将回溯帧一并纳入，使模型学会“提前规避危险”。

### 第三遍：主动学习式精筛

- **目的**：在候选池中优中选优，最大化标注价值。
- **做法**：
  1. 使用预训练的 VAD 基础模型，在候选池上推理，计算每帧的规划不确定性（多次 dropout 推理的轨迹方差）。
  2. 综合打分：`样本价值分 = α × 危险严重度 + β × 规划不确定性`。
  3. 按价值分降序排列，取前 N 帧（如 5000~10000）送 LLM 进行常识标注。

## 五、LLM 常识蒸馏

1. **“案情简报”自动生成**：将精筛出的帧的结构化信息转换为自然语言描述，清晰说明自车状态、道路结构、周围物体的位置/速度/意图/可见度。
2. **LLM 推理**：使用纯文本 LLM（如 InternLM2 20B、DeepSeek-V2-Lite），以固定 Prompt 输出标准化 JSON：
   - `risk_level`：1~10
   - `primary_causal_object`：最关键的危险诱因（物体 ID）
   - `hazard_type`：危险类型
   - `commonsense_reasoning`：自然语言常识推理
   - `attention_focus`：对每个周围物体的注意力权重列表
3. **质量保证**：先人工校验少量样本，确认输出稳定后批量标注全部高价值样本，形成常识标签库。

## 六、域迁移应对（Domain Randomization）

- **问题**：nuScenes 真实图像训练的 VAD，直接评测 CARLA/Bench2Drive 仿真图像会导致感知崩溃。
- **方案**：训练时加入 5%–10% 经多样化增强（颜色抖动、高斯模糊、噪声、CutOut、MixUp）的 CARLA 仿真图片，与 nuScenes 真实图片混合训练。
- **效果**：模型视觉特征自然获得跨域鲁棒性，无需修改模型结构。

## 七、模型结构（基于 VAD）

基于 VAD 的向量化端到端规划模型，在其规划 decoder 之上并连三个输出头：

**三个输出头共享 VAD 的视觉 backbone 和规划 decoder 特征，联合训练。**

### 7.1 规划头（VAD 原生）

- **输出**：未来轨迹点序列 (T, 2)，T 为预测时间步数。
- **监督信号**：nuScenes 数据集中的专家轨迹 GT 标签。
- **损失函数**：原始 VAD 的轨迹预测损失（L1 或 L2）。
- **特点**：输出确定性轨迹，而非概率分布。这是 VAD 与 VAD2 的核心区别。

### 7.2 物理硬约束头

- **输入**：规划 decoder 特征。
- **输出**：四个关键物理风险信号。

| 输出项 | 维度 | 物理含义 | 标签来源 |
|:---|:---|:---|:---|
| 多物体 TTC 下限 | (N,) | 与每个周围物体的安全 TTC 阈值 | 离线运动学公式计算 |
| 最大安全曲率 κ_max | 1 | 当前速度下防止翻车/侧滑的曲率上限 | μ·g / v² |
| 最大安全减速度 a_brake_max | 1 | 防止追尾/失控的制动极限 | 预设物理常数 |
| 紧急制动概率 P_AEB | 1 | 当前是否需要立刻触发 AEB | TTC < 0.5s 且无避让空间 → 1 |

- **训练损失**：`L_physical = MSE(TTC) + MSE(κ_max) + MSE(a_brake_max) + BCE(P_AEB)`

### 7.3 常识蒸馏头

- **输入**：规划 decoder 特征。
- **输出**：多任务常识预测。

| 子任务 | 输出维度 | 监督信号 |
|:---|:---|:---|
| 危险等级预测 | 1（标量，1~10） | LLM 输出的 risk_level |
| 关键诱因识别 | (N,) 分类 logits | LLM 输出的 primary_causal_object |
| 注意力分布对齐 | (N,) 权重向量 | LLM 输出的 attention_focus |

- **训练损失**：`L_commonsense = MSE(危险等级) + CrossEntropy(关键诱因) + KL_divergence(注意力分布)`

## 八、训练样本与损失函数

### 8.1 训练样本组装

- **输入**：混合图像集（nuScenes 真实图片 + 增强仿真图片，比例约 9:1）。
- **监督信号**：
  - 物理风险标签（多物体 TTC 下限、κ_max、a_brake_max、P_AEB）。
  - LLM 常识标签（risk_level、primary_causal_object、attention_focus）。
  - 专家轨迹标签（VAD 原生规划头）。

### 8.2 联合损失函数
- L_total = L_planning + λ₁ L_physical + λ₂ L_commonsense

- L_planning：VAD 原生轨迹预测损失。
- L_physical：物理约束头多任务损失。
- L_commonsense：常识蒸馏头多任务损失。
- λ₁、λ₂：初始设为 0.5，后续根据实验调整。

### 8.3 训练流程

1. 加载 VAD 官方预训练权重作为初始化。
2. 冻结视觉 backbone 前几层，仅微调 decoder 部分和两个新增辅助头。
3. 使用混合数据集联合训练三个输出头。
4. 辅助头仅在训练时计算额外损失，推理时无额外延迟。

## 九、推理过程

1. 视觉 backbone 提取特征 → decoder 特征。
2. **物理硬约束头**输出四个风险信号。
3. **规划头**输出确定性未来轨迹。
4. **安全检查与轨迹修正**：
   - 计算规划轨迹与每个周围物体的预测 TTC，若低于硬约束头输出的 TTC 下限 → 触发紧急制动。
   - 规划轨迹曲率 > κ_max → 裁剪至 κ_max。
   - 规划轨迹加速度 < a_brake_max → 裁剪至 a_brake_max。
   - P_AEB > 0.9 → 直接覆盖，执行 AEB。
5. 输出经过安全约束的最终轨迹。

## 十、评测方案

### 10.1 开环评测（nuScenes 验证集）

- 危险等级预测准确率（与 LLM 标签对比）。
- 关键危险诱因识别准确率。
- 注意力分布与 LLM 标签的一致性（KL 散度/余弦相似度）。
- VAD 原始规划指标（ADE、FDE），证明加辅助头未损害驾驶水平。

### 10.2 闭环评测（CARLA / Bench2Drive）

- 自定义长尾测试场景（鬼探头、紧急切入、闯红灯、盲区侵入等）。
- **核心指标**：碰撞率、通过率、舒适度（加加速度）。
- **对比基线**：
  - 纯 VAD
  - VAD + 物理硬约束头
  - VAD + 常识蒸馏头
  - VAD + 物理硬约束头 + 常识蒸馏头（完整模型）

### 10.3 消融实验

逐一去除以下模块，验证独立贡献：
- 物理硬约束头
- 常识蒸馏头
- 时间回溯机制
- Domain Randomization

## 十一、核心贡献

1. **问题解耦**：首次将端到端驾驶的安全问题分解为物理安全（确定性）与驾驶常识（模糊性）两个独立维度，并提出异构知识注入框架。

2. **物理风险探测器设计**：定义了多物体 TTC 下限、曲率/减速度边界、紧急制动概率四维输出，直接回答“会不会撞、会不会翻、要不要立刻刹停”，超越了简单的轨迹后处理。

3. **结构化常识蒸馏范式**：不给 LLM 看图像，而是提供无噪声的结构化“案情简报”，消除 VLM 的几何幻觉，让 LLM 专注于常识推理。

4. **协同内部化机制**：通过常识蒸馏头引导特征学习，使物理约束头从“外部强制”变为模型内部可理解的安全边界，解决了多任务冲突。

5. **系统化数据挖掘管线**：三阶段递进式长尾危险样本挖掘（全量索引 + 危险基元初筛 + 主动学习精筛），为安全常识注入提供可复现、可扩展的数据基础。

## 十二、可参考的关键论文

| 论文 | 可借鉴内容 |
|:---|:---|
| VAD (ICCV 2023) | 向量化端到端规划基底 |
| LAV (CVPR 2022) | 场景标签化与自动检索 |
| ActiveAD (ECCV 2024) | 规划导向主动学习 |
| Domain Randomization (Tobin et al., IROS 2017) | 域随机化基础 |

## 十三、目录结构
```
SafeDrive-LLM/
├── configs/                    # 配置文件
│   ├── base_config.yaml        # 基础配置（路径、模型参数、训练超参）
│   ├── vad2_config.py          # VAD 原生配置（从官方仓库迁移）
│   ├── data_mining.yaml        # 数据挖掘管线参数
│   └── llm_config.yaml         # LLM 调用配置（模型名、API 地址、Prompt 模板）
│
├── data/                       # 数据相关
│   ├── raw/                    # 原始数据
│   │   ├── nuScenes/           # nuScenes 数据集（软链接）
│   │   └── can_bus/            # CAN 总线数据（已就位）
│   ├── processed/              # 处理后数据
│   │   ├── meta_db.pkl         # 全量场景标签索引元数据库（待生成）
│   │   ├── hazard_candidates.pkl  # 候选危险样本池（待生成）
│   │   ├── high_value_samples.pkl # 主动学习精筛后的高价值样本（待生成）
│   │   └── llm_labels.json     # LLM 常识标注结果（待生成）
│   └── carla/                  # CARLA 仿真数据
│       ├── collect_data.py     # 采集仿真图片与 GT 脚本
│       └── sim_images/         # 存储采集的仿真图像
│
├── vad2/                       # VAD 系列模型代码库（基于官方仓库）
│   ├── mmdet3d_plugin/         # 模型核心代码（插件架构）
│   │   ├── VAD/                # VAD 模型实现（VADHead、VADTransformer）
│   │   ├── bevformer/          # BEV 特征提取
│   │   ├── datasets/           # 数据集适配（nuScenes VAD 格式）
│   │   ├── models/             # 检测/分割头
│   │   └── core/               # 核心工具
│   ├── configs/VAD/            # VAD 训练/评测配置
│   │   ├── VAD_tiny_stage_1.py # 阶段 1：感知+预测（48 epoch）
│   │   ├── VAD_tiny_stage_2.py # 阶段 2：规划（12 epoch，加载阶段 1 ckpt）
│   │   ├── VAD_base_stage_1.py
│   │   ├── VAD_base_stage_2.py
│   │   └── VAD_*_e2e.py        # 端到端配置（不推荐）
│   ├── models/                 # 模型定义
│   ├── datasets/               # 数据加载器
│   ├── tools/                  # 训练/评测/可视化脚本
│   ├── checkpoints/            # 模型权重目录（.pth 存储在外部 HDD）
│   ├── VADv2_head.py           # VADv2 模型实现（备选基底）
│   └── VADv2_config_voca4096.py  # VADv2 配置（备选基底，需 CARLA 数据）
│
├── mining/                     # 数据挖掘管线（待实现）
│   ├── build_meta_db.py        # 第一遍：全量预计算场景标签索引
│   ├── hazard_primitives.py    # 危险基元库定义与规则引擎
│   ├── filter_hazards.py       # 第二遍：运行规则筛选候选池 + 时间回溯
│   ├── active_selection.py     # 第三遍：主动学习精筛（需 VAD 推理）
│   └── utils.py                # 辅助函数（射线检测、TTC 计算等）
│
├── llm/                        # LLM 常识标注（待实现）
│   ├── prompt_templates.py     # 案情简报生成与 Prompt 模板
│   ├── generate_labels.py      # 批量调用 LLM 标注
│   ├── finetune_llm/           # （可选）LLM 小样本微调
│   │   ├── train_data.json     # 人工标注的 50-100 条样本（待创建）
│   │   ├── finetune_lora.py    # LoRA 微调脚本（待创建）
│   │   └── merged_model/       # 合并后权重
│   └── utils.py                # JSON 解析、输出校验
│
├── model/                      # 模型改造（基于 VAD 系列，待实现）
│   ├── vad2_with_heads.py      # 添加硬约束头 + 常识蒸馏头的模型定义
│   ├── losses.py               # 联合损失函数（L_planning + λ1 L_hard + λ2 L_common）
│   └── dataloader.py           # 混合数据加载器（nuScenes + 增强仿真）
│
├── train/                      # 联合训练（待实现）
│   ├── train_joint.py          # 联合训练主脚本
│   ├── train_utils.py          # 优化器、scheduler、日志
│   └── logs/                   # TensorBoard 日志
│
├── eval/                       # 评测（待实现）
│   ├── openloop/               # 开环评测
│   │   ├── run_openloop.py     # 在 nuScenes 验证集评测脚本
│   │   └── eval_metrics.py     # 危险等级、诱因准确率、注意力一致性
│   ├── closedloop/             # 闭环评测
│   │   ├── carla_runner.py     # CARLA 闭环评测主脚本（集成模型）
│   │   └── scenarios/          # 自定义长尾测试场景配置文件
│   └── ablation/               # 消融实验配置与结果
│       ├── run_ablation.sh     # 批量运行消融实验
│       └── results/            # 消融结果 CSV/图表
│
├── notebooks/                  # Jupyter Notebook 探索性分析（待创建）
│   ├── data_stats.ipynb        # 数据分布、危险样本统计
│   └── llm_output_analysis.ipynb  # LLM 标注质量检查
│
├── charts/                     # 评测可视化图表
│
├── scripts/                    # 辅助脚本
│   ├── setup_env.sh            # 环境配置
│   ├── download_deps.sh        # 下载依赖
│   └── run_pipeline.sh         # 一键运行完整管线
│
├── docs/                       # 设计文档
│   ├── design.md               # 方案设计思路（待创建）
│   └── experiment_log.md       # 实验记录与超参记录（待创建）
│
├── requirements.txt            # Python 依赖
├── .gitignore
├── AGENTS.md                   # 开发者指南（路径、命令、环境）
├── project_struct.md           # 本文件：项目整体方案与结构
└── vad_tiny_eval_report.md     # VAD-Tiny 官方基线评测报告
```